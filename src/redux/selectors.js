export const selectWishlist = (state) => state.wishlistState.wishlist;
// export const selectSlugs = (state) => state.slugState.slugs;
export const showAccordianState = (state) => state.accordianState.openIndex;