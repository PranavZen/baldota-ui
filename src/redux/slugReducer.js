// import { SET_SLUGS } from "./actions";

// const initialSlugState = {
//   slugs: {},
// };

// const slugReducer = (state = initialSlugState, action) => {
//   switch (action.type) {
//     case SET_SLUGS:
//       return {
//         ...state,
//         slugs: action.payload,
//       };
//     default:
//       return state;
//   }
// };

// export default slugReducer;
